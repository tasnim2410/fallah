# Copy to .env and change the values before putting the site online.
# The server reads these from the process environment.

PORT=3000

# Password for /admin.html (the seller's dashboard).
FALLAH_ADMIN_PASSWORD=change-me

# Seller phone shown in the header / order confirmation (local Tunisian format).
FALLAH_SHOP_PHONE=+21600000000
